export type FormatedDespesas = {
    grossArray: [number, number, string][],
    netArray: [number, number, string, string][],
    voucherArray: [number, number, string][],
    pixArray: [number, number, string][],
    grossTotal: number,
    netTotal: number,
    voucherTotal: number,
    pixTotal: number
}